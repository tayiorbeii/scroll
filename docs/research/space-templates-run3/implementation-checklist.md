# Implementation Checklist: save a window-manager layout as a template with placeholder slots and restore it by binding freshly launched windows to those slots

## Dependencies
- [ ] Install or verify `wlroots`.
- [ ] Install or verify `wayland`.

## Environment
- [ ] Confirm environment variables against the selected repos' README/env docs.

## Files to create or modify
- [ ] `package.json` — dependency manifest (no direct proof; verify against the selected repos).
- [ ] `README.md` — save a window-manager layout as a template with placeholder slots and restore it by binding freshly launched windows to those slots implementation file (no direct proof; verify against the selected repos).

## Implementation flow
- [ ] No implementation flow was extracted; follow the selected repos' evidence anchors.

## Tests
- [ ] Add unit tests for the new flow.
- [ ] Add one integration / e2e path.
- [ ] Run a manual smoke test.

## Verification & rollout
- [ ] Re-check every accepted repo's evidence anchors against live source before relying on them.
- [ ] Land the change behind small, isolated commits so a single revert removes it.
- [ ] Update README / internal docs and resolve or explicitly accept open questions.
