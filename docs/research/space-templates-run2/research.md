# Research: Tiling manager save restore window layout (INCONCLUSIVE)

> ⚠ **This run is INCONCLUSIVE.** No fully evidence-backed conclusion was reached.
> The sections below describe what was searched and why proof remained incomplete — they are
> diagnostics, not verified findings. Do not present this as completed research.

## Goal

Tiling window manager layout save and restore with placeholder windows: i3-resurrect i3 swallow layout files, sway swaymsg append_layout, swayr window matching, niri scrollable-tiling, PaperWM session restore, Qtile saved layouts, awesome-wm tags restore, xdg-session-management Wayland protocol

## Status

- **Status:** `inconclusive`
- **Reasons:** no_selected_repositories, no_evidence_anchors
- Candidates evaluated: 15
- Repositories selected: 0
- Evidence anchors: 6

## Summary

Run `run_tiling_manager_save_window_1789617857250` evaluated 15 candidate(s), selected
0 repository/repositories, and captured 6
evidence anchor(s), but did not satisfy the complete proof contract. This is
**not** proof that the answer is negative. Treat these artifacts as diagnostics,
not completed findings.

## What was searched

- [high_precision] "Tiling" "manager" — Find files combining the two most distinctive feature concepts.
- [repo_search] Tiling manager save restore window layout — Discover repos by name/topic/readme when code search misses.

## Open questions (unresolved)

- Were any files actually read for the candidates? Are likelyFiles/proof signals correct?

## Recovery steps

- Inspect research.md "What was searched" to confirm probes targeted the right entities.
- If discovery missed the project, add the official repo/docs URL as an explicit hint or candidate.
- Provide structured hints (requiredConcepts, likelyFiles, proofRequirements) for novel domains.
- Re-run into a new output directory and compare; do not overwrite inconclusive artifacts.

## Diagnostics (warnings)

- missingProof: comfies/custard was not proof-read (maxReposToProve reached).
- missingProof: Excedrin/pytyle2 was not proof-read (maxReposToProve reached).
- missingProof: FlightGear/simgear was not proof-read (maxReposToProve reached).
- missingProof: GMkonan/dotfiles was not proof-read (maxReposToProve reached).
- missingProof: GNOME/gnome-shell was not proof-read (maxReposToProve reached).
- missingProof: kitnil/notes was not proof-read (maxReposToProve reached).
- missingProof: mate-desktop/marco was not proof-read (maxReposToProve reached).
- missingProof: mate-desktop/mate-control-center was not proof-read (maxReposToProve reached).

## Skeptic notes

- An inconclusive run must never be presented as completed research.
- Re-run after fixing the highest-priority reason above before drawing any conclusion.
