# Audit — GitHub issues vs. planning documents (2026-09-17)

Pre-publication-of-implementation audit: issues [#1–#6](https://github.com/tayiorbeii/scroll/issues/1)
checked against [plan.md](plan.md) v2.1, the v2.1-final slice graph
(`pi-plan/graph-space-templates-execution-graph-v2.1-final.md`), and
[implementation-checklist.md](implementation-checklist.md).

## Result: PASS (6/6)

| Issue | Slice | Invariants verified | Labels |
|---|---|---|---|
| #1 | G1 model | unique slot rule; no view/container pointers in durable state; Meson registration; legacy `sway_space` untouched | enhancement, c, space-templates |
| #2 | G2 JSON | JSON-path errors; atomic temp+rename; corrupt-file isolation; XDG path; unknown keys tolerated | enhancement, c, space-templates, persistence |
| #3 | G3 apply | validate→sweep→arrange→dissolve; `SPACE_RESTORE_HIDE` sweep; error names affected slots; zero mutation on failure; never an empty leaf / fake client / pending session | enhancement, c, space-templates, compositor, tests |
| #4 | G4 IPC/commands | dedicated `get_space_template`/`apply_space_template`; legacy `get_spaces` unchanged; **no `fallback`/`retry`/`staging` fields** | enhancement, c, space-templates, ipc |
| #5 | G5 Lua | `(nil, error)` naming missing slots; no bind/commit/cancel machinery; fill-then-retry + launcher examples are plain userland scripts | enhancement, c, space-templates, lua, documentation, tests |
| #6 | G6 docs/hardening | docs describe only the final contract; anti-feature term checks; no publication/PR/merge without owner | documentation, space-templates, tests, release |

## Notes

- Automated scan of issue bodies for un-negated anti-feature terms flagged two
  lines in #5; both reviewed manually and confirmed correct (they describe the
  **userland** placeholder/fallback patterns per resolved H-02, not compositor
  features).
- Local staleness scan of `plan.md` / `implementation-checklist.md` for retired
  v2 identifiers (`space_template_session`, `space_template_bind/commit/
  cancel`): clean.
- Dependency chain in issues matches the graph: #1 → #2 → #3 → #4 → #5 → #6
  (with #2 unlocking #4's export half per the graph).
- Distribution rule in force: these planning docs stay on the
  `planning/space-templates-docs` branch of the fork; upstream PR branches are
  cut fresh from upstream `master` with cherry-picked implementation commits
  only.
